import MainCall from "@/components/MainCall";
import { memo } from "react"


const CallCredentials = (): JSX.Element => {


    return (
        <MainCall/>
    )
}

export default memo(CallCredentials);